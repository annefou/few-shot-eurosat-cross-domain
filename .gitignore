# Data (downloaded automatically)
data/
!data/.gitkeep

# Model weights (large, regenerate by re-running the experiments)
results/*.pth

# Figures ARE committed: results/*.png from the experiment scripts and
# figures/*.png derived from the committed results JSON. They are the record
# of what was found, and the Jupyter Book needs them at build time.

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Jupyter notebooks generated from Jupytext
*.ipynb
